version: "3.8"
services:
  server:
    build: ./movie-gallery-server
    container_name: server_c
    ports:
      - "5000:5000"
  client:
    build: ./movie-gallery
    container_name: client_c
    ports:
      - "8081:8081"
    stdin_open: true
    tty: true
